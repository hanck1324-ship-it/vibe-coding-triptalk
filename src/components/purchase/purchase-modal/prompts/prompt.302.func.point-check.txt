아래의 조건을 모두 적용하여, 아래의 요구사항을 모두 구현할 것.
구현 결과를 체크리스트로 반환할 것.

==============================================

조건-커서룰) 아래의 커서룰을 적용하여 작업하고, 이 작업이 끝나면 해당 rules 적용 결과를 체크리스트로 반환할 것.
            - @01-common.mdc
            - @04-func.mdc

==============================================

조건-선행작업) prompt.301.func.modal-connect.txt가 먼저 완료되어야 합니다.

조건-파일경로) 수정할 파일 목록
            - src/components/purchase/detail/index.tsx (상세페이지)
            - src/components/purchase/detail/hook.ts (상세페이지 훅)

==============================================

핵심요구사항) 구매 시 포인트를 체크하고, 부족하면 포인트 부족 모달을 표시할 것.

        1) 상세페이지 훅 구현 (src/components/purchase/detail/hook.ts)

            - 포인트 부족 모달 상태 관리 추가
              * isPointAlertOpen: boolean
              * useState로 상태 선언

            - 포인트 정보 상태 관리 추가
              * currentPoint: number (사용자 보유 포인트)
              * 초기값: 50000 (테스트용 하드코딩)
              * 나중에 TODO: API에서 가져오기로 주석 작성

            - 포인트 부족 모달 핸들러 추가
              * handleClosePointAlert()
                - setIsPointAlertOpen(false)

            - handlePurchaseConfirm 함수 수정 (기존 onConfirm 콜백 내용 교체)
              * 구현 로직:

                const handlePurchaseConfirm = () => {
                  // 1단계: 포인트 체크
                  const requiredPoint = accommodation?.price || 0;

                  if (currentPoint < requiredPoint) {
                    // 포인트 부족
                    setIsPointAlertOpen(true);
                    setIsPurchaseModalOpen(false); // 구매 모달 닫기
                    return; // 프로세스 중단
                  }

                  // 2단계: 포인트 충분 (다음 프롬프트에서 구현 예정)
                  console.log("포인트 충분 - 구매 진행");
                  setIsPurchaseModalOpen(false);
                };

            - return 값에 추가
              * handlePurchaseConfirm (새로 만든 함수)
              * isPointAlertOpen
              * handleClosePointAlert
              * currentPoint
              * requiredPoint: accommodation?.price || 0

        2) 상세페이지 UI 구현 (src/components/purchase/detail/index.tsx)

            - 훅에서 값 가져오기 (기존 useDetail 수정)
              * const {
                  accommodation,
                  loading,
                  isPurchaseModalOpen,
                  handleOpenPurchaseModal,
                  handleClosePurchaseModal,
                  handlePurchaseConfirm, // 새로 추가
                  isPointAlertOpen, // 새로 추가
                  handleClosePointAlert, // 새로 추가
                  currentPoint, // 새로 추가
                  requiredPoint // 새로 추가
                } = usePurchaseDetail();

            - PurchaseModal의 onConfirm 수정
              * 기존: onConfirm={() => { console.log(...); handleClose(); }}
              * 수정: onConfirm={handlePurchaseConfirm}

            - PointAlertModal 컴포넌트 추가
              * import: import PointAlertModal from "../point-alert-modal"
              * 위치: PurchaseModal 아래
              * 코드:
                <PointAlertModal
                  isOpen={isPointAlertOpen}
                  onClose={handleClosePointAlert}
                  currentPoint={currentPoint}
                  requiredPoint={requiredPoint}
                />

        3) 포인트 체크 플로우

            구매하기 버튼 클릭
                    ↓
            handlePurchaseConfirm 실행
                    ↓
            포인트 체크
                    ↓
            ┌───────┴───────┐
            │               │
      포인트 부족      포인트 충분
            │               │
            ↓               ↓
    구매 모달 닫기    콘솔 로그 출력
            ↓          구매 모달 닫기
    포인트 부족         (다음 프롬프트에서
    모달 표시           API 구현 예정)
            ↓
    "충전하러 가기" 클릭
            ↓
    /myPage?tab=point

        4) 테스트 체크리스트

            [포인트 부족 시나리오]
            - [ ] 보유 포인트를 50000원으로 설정했는가?
            - [ ] 숙박권 가격이 보유 포인트보다 높은 경우 테스트했는가?
            - [ ] 구매하기 버튼 클릭 시 구매 모달이 닫히는가?
            - [ ] 포인트 부족 모달이 바로 표시되는가?
            - [ ] 포인트 부족 모달에 보유 포인트가 올바르게 표시되는가?
            - [ ] 포인트 부족 모달에 필요 포인트가 올바르게 표시되는가?
            - [ ] 포인트 부족 모달에 부족 포인트가 올바르게 계산되는가?

            [포인트 부족 모달 동작]
            - [ ] 포인트 부족 모달의 "닫기" 버튼이 작동하는가?
            - [ ] 포인트 부족 모달의 "충전하러 가기" 버튼이 작동하는가?
            - [ ] "충전하러 가기" 클릭 시 /myPage?tab=point로 이동하는가?
            - [ ] 마이페이지에서 포인트 충전 탭이 자동으로 활성화되는가?

            [포인트 충분 시나리오]
            - [ ] 보유 포인트를 높게 설정하여 테스트했는가?
            - [ ] 포인트 충분 시 콘솔에 "포인트 충분 - 구매 진행" 로그가 출력되는가?
            - [ ] 포인트 충분 시 포인트 부족 모달이 표시되지 않는가?
            - [ ] 포인트 충분 시 구매 모달이 닫히는가?

            [프로세스 중단 확인]
            - [ ] 포인트 부족 시 다음 단계로 진행되지 않는가?
            - [ ] 포인트 부족 시 페이지 이동이 발생하지 않는가?

==============================================

참고사항)
1. 이 프롬프트는 포인트 체크만 담당합니다.
2. 사용자 포인트는 테스트용으로 50000원을 하드코딩합니다.
3. 실제 API 연동은 나중에 TODO로 남겨둡니다.
4. 구매 API 호출은 prompt.303.func.purchase-api.txt에서 구현합니다.
5. 포인트 충분한 경우는 콘솔 로그만 출력하고 다음 프롬프트로 넘깁니다.
